[project]
name = "experanto-edge-agent"
version = "0.4.1"
description = "Experanto Edge agent — bridges a local datalogger (Solar-Log Base 2000) to Experanto over MQTT."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "paho-mqtt>=1.6",
    "requests>=2.25",
    "PyYAML>=5.4",
    "cryptography>=41",     # Ed25519 verification of signed OTA releases (E5)
    "bcrypt>=4.0",          # login "user" al datalogger per il dettaglio per-inverter (143)
]

[project.optional-dependencies]
dev = ["pytest>=7"]

[project.scripts]
experanto-edge = "experanto_edge.main:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["experanto_edge*"]
